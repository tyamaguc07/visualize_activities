#!/usr/bin/env ruby

require "visualize_activities"
require "thor"

class VisualizeActivitiesCLI < Thor
  desc 'exec target target_date', 'fetch issues activities'
  def exec(target, target_date)
    VisualizeActivities.execute(target, target_date)
  end
end

VisualizeActivitiesCLI.start(ARGV)
